namespace solution {

/**
 * Removes consecutively duplicate characters in a string (in place).
 *
 * @param str pointer to the beginning of a string.
 */
void remove_duplicates(char * str);

} // namespace solution
